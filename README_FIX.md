# yohaku 表示崩れ修正版

現在のGitHubリポジトリでは、ファイルがフォルダ構造ではなくルート直下にアップロードされています。
そのため、`css/style.css` や `assets/images/...` を読みに行けず、CSSと画像が表示されていません。

この修正版は、すでにGitHub上にあるルート直下ファイル名に合わせて、パスを書き換えたものです。

## 今回アップロードするもの

以下3つだけを上書きアップロードしてください。

- index.html
- style.css
- script.js

特に重要なのは `index.html` です。
